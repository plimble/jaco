{
  "root": "packages/sagas",
  "sourceRoot": "packages/sagas/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/sagas/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/sagas"],
      "options": {
        "jestConfig": "packages/sagas/jest.config.js",
        "passWithNoTests": true
      }
    },
    "build": {
      "executor": "@nrwl/node:package",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/sagas",
        "tsConfig": "packages/sagas/tsconfig.lib.json",
        "packageJson": "packages/sagas/package.json",
        "main": "packages/sagas/src/index.ts",
        "assets": ["packages/sagas/*.md"]
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "postTargets": ["sagas:deploy"],
        "skipRootChangelog": true,
        "skipProjectChangelog": true
      }
    },
    "deploy": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "restricted"
      }
    }
  },
  "tags": []
}
